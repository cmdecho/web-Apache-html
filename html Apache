<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Web Server Rizky Maulana</title>
    <style>
        /* Gaya dasar untuk background */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%);
            color: #f8fafc;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Desain Kartu Utama */
        .container {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            padding: 2rem;
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            text-align: center;
            max-width: 400px;
            width: 90%;
        }

        h1 {
            color: #38bdf8;
            margin-bottom: 0.5rem;
            font-size: 1.8rem;
        }

        .name {
            font-weight: bold;
            color: #f1f5f9;
            font-size: 1.2rem;
            margin-bottom: 1.5rem;
            display: block;
        }

        p {
            line-height: 1.6;
            color: #cbd5e1;
        }

        /* Badge Status */
        .status-badge {
            display: inline-block;
            background: #22c55e;
            color: white;
            padding: 5px 15px;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: bold;
            margin-top: 1rem;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.7; }
            100% { opacity: 1; }
        }

        footer {
            margin-top: 2rem;
            font-size: 0.75rem;
            color: #64748b;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Web Server Local</h1>
        <span class="name">Rizky Maulana</span>
        
        <p>Selamat datang! Server ini sedang berjalan di sistem operasi <strong>Linux Mint</strong> menggunakan <strong>Apache2</strong>.</p>
        
        <div class="status-badge">● SERVER ACTIVE</div>

        <footer>
            Dikelola secara mandiri oleh Rizky &bull; 2026
        </footer>
    </div>

</body>
</html>
